# Quickstart: 验证微信小程序地图 marker 渲染崩溃修复

**Feature**: 014-fix-weapp-map-crash | **Date**: 2026-05-22

本清单在实现完成后执行，对应 spec 的 SC-001 ~ SC-006。前端无单测设施，全部为人工核验。

## 前置

- 微信开发者工具**基础库用稳定版**（非灰度版——灰度 3.16.1 会带来无关的 `Error: timeout`）
- 微信开发者工具「详情 → 本地设置」勾选「不校验合法域名、web-view、TLS 版本…」（否则后端请求被拦）
- 后端已起、点位数据能加载（spec-013 已打通）

## 构建

- [ ] `build:weapp` 成功，无编译错误
- [ ] `build:h5` 成功，无编译错误

## A. 小程序地图崩溃修复（SC-001 / SC-002）

- [ ] 微信开发者工具打开小程序首页，地图正常渲染
- [ ] Console **无** `Cannot read property 'lat' of undefined` 渲染层崩溃（SC-002，0 次）
- [ ] 当前区域有点位数据时，地图上**能看到点位 marker**（SC-001）

## B. 点位数量三场景都不崩（SC-003 / FR-005）

- [ ] **0 个点位**（如刚打开未搜索，或搜了无结构化结果）——地图不崩、不白屏
- [ ] **1 个点位**——地图不崩，该 marker 正常显示
- [ ] **多个点位**——地图不崩，所有 marker 显示、视野自动框住它们
- [ ] 顺带确认：1 个点位场景下视野/缩放正常（验证 research.md「≤1 是否崩」的悬留问题）

## C. marker 交互（SC-004）

- [ ] 点击地图上某 marker → 打开该点位详情抽屉，内容正确（与 spec-012 设计一致）

## D. H5 零回归（SC-005 / US2）

- [ ] H5 首页地图、marker、点击交互与改动前肉眼一致
- [ ] H5 搜索后地图自动飞到结果范围（高德 `setFitView`）行为不变

## 结论

- [ ] 全部通过 → spec-014 达成；把 research.md「最大不确定性」一项更新为「已运行时核验」
- [ ] 若 A 仍崩 → 根因推断有误，回 research.md 重查（按 FR-002，不接受带病往下做）
