# Contract: 小程序平台差异清单 —— 文档结构模板

spec-009 产出的 `docs/mvp-backlog/小程序平台差异清单.md` 必须遵循此结构，
确保后续 spec 能直接消费。

---

## 文档结构

```markdown
# 小程序平台差异清单

> 来源：spec-009 编译 + 运行实测（2026-05-21）
> 用途：7.1 后续 spec（地图适配 / 信源外链 / 后端网络层 / 导航 / AI 标注）的范围输入

## 一、编译期问题（build:weapp 暴露）

| # | 名称 | 现象 | 严重度 | 建议归属 spec |
|---|------|------|--------|---------------|
| C1 | ... | ... | 阻断首页/功能缺失/体验降级 | ... |

## 二、运行期问题（微信开发者工具暴露）

| # | 名称 | 类别 | 现象 | 严重度 | 建议归属 spec |
|---|------|------|------|--------|---------------|
| R1 | 高德地图不渲染 | 地图 | ... | 功能缺失 | spec-010 地图适配 |

## 三、按后续 spec 归类汇总

### → 地图适配 spec（spec-010）
- R1 高德地图不渲染
- ...

### → 信源外链 spec
- ...

### → 后端 API 网络层 spec
- ...

### → AI 内容标注 spec
- ...

## 四、本 spec 内已隔离的「阻断首页」项

| 名称 | 隔离方式 |
|---|---|
| ... | ... |
```

---

## 字段约束

- **严重度**必须是三选一：`阻断首页` / `功能缺失` / `体验降级`
- `阻断首页` 的项 **MUST** 在 spec-009 内被隔离（FR-004），并登记在「四、已隔离项」
- `功能缺失` / `体验降级` 的项必须有「建议归属 spec」，不能悬空
- 「三、按后续 spec 归类汇总」**MUST** 覆盖「一、二」里所有非「已隔离」的条目——即每个差异都要有去处

---

## 验收对照（spec.md SC-003 / SC-005）

- SC-003：清单覆盖所有当前能发现的不兼容点
- SC-005：7.1 剩余每块工作都能对应到清单条目 → 「三、按后续 spec 归类汇总」就是这个映射
