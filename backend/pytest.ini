[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
# 所有 async fixture/test 共用 session 级 event loop
# 避免 SQLAlchemy async engine 跨 loop "attached to a different loop" 错
asyncio_default_fixture_loop_scope = session
